using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ChickenCode : MonoBehaviour
{
    public int health = 2;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //Chicken switches between walking and standing depending on timer.
        //If hit by player, chicken will run in opposite direction of player for 5 seconds.

    }

    //Take damage function
    //Upon death, destroy chicken gameobject and drop 1 meat.

}
